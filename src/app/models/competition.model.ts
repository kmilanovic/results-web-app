export interface Competition {
  area: object;
  code: string;
  currentSeason: object;
  emblemUrl: string;
  id: number;
  lastUpdated: string;
  name: string;
  numberOfAvailableSeasons: number;
  plan: string;
}
